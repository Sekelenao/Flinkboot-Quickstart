<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.sekelenao</groupId>
    <artifactId>flinkboot-quickstart</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>Flinkboot Quickstart</name>
    <description>Zero-boilerplate Apache Flink quickstart application powered by Flinkboot</description>

    <modules>
        <module>job</module>
        <module>producer</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <dependency.flinkboot.version>0.2.0-1.20</dependency.flinkboot.version>
        <dependency.logback.version>1.5.16</dependency.logback.version>
        <plugin.maven-compiler.version>3.13.0</plugin.maven-compiler.version>
        <plugin.maven-shade.version>3.6.0</plugin.maven-shade.version>
        <plugin.maven-surefire.version>3.5.2</plugin.maven-surefire.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Flinkboot BOM : Manages Flink 1.20, Kafka connectors, Jackson, Log4j, JUnit, provided scopes -->
            <dependency>
                <groupId>io.github.sekelenao</groupId>
                <artifactId>flinkboot</artifactId>
                <version>${dependency.flinkboot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Internal module -->
            <dependency>
                <groupId>io.github.sekelenao</groupId>
                <artifactId>job</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${plugin.maven-compiler.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <release>${java.version}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${plugin.maven-surefire.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${plugin.maven-shade.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>